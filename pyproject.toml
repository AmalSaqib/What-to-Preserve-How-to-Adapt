[tool.ruff]
target-version = "py39"
line-length = 100
extend-exclude = ["nnunet_ext/ignored", "storage"]

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["reproducibility/tests"]
addopts = "-q"
